feat: implement all 5 UX enhancements for conversion + clarity

1. ConversionGateCard — personalised blurred tile previews using real
   scan data (role, city, top at-risk skill, salary bleed, peer pct,
   pivot role); savings badge (Save ₹1,601) anchored to annual CTA;
   upgraded monthly button with line-through anchor; trust bar
   (Razorpay · 7-day refund · cancel anytime · data privacy) inline
   under CTAs.

2. DoomClockCard — auto-captures share card image on mount via
   html2canvas after 800ms delay; silent failure leaves manual
   button as fallback; manual button label updated.

3. AIDossierReveal — free-user gate: show score + verdict headline/body
   only; moat/threat skill names, replaceability text, bullets, AIRMM
   bars, market context, and AI dossier collapsible all gated behind
   isProUser prop; blurred ghost AIRMM preview + gradient fade + Pro
   teaser CTA shown to free users instead.

TypeScript: 0 errors.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
