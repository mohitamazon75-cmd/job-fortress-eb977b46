fix: pre-launch QA sweep — 6 critical/high issues resolved

Critical:
- activate-subscription: .eq("id") → .eq("user_id") — subscription was
  silently failing for every paying user (profile FK mismatch)
- ConversionGateCard: onUpgrade('scan') → onUpgrade('month') — 'scan'
  tier removed when pricing changed; upgrade CTA was broken
- Index.tsx: pass isProUser={isProUser} to InsightCards — Pro users
  were locked into 3-card free arc after paying ₹1,999

High:
- razorpay-webhook: add subscription.activated, subscription.charged,
  subscription.cancelled, subscription.completed handlers — monthly
  renewals were being silently ignored, subscriptions never extended
- SkillCrisisResponseCenter: add type="button" to 3 buttons (lines
  227, 270, 473) — missing type defaulted to submit, could fire forms
- ProUpgradeModal: add defaultTier prop + useEffect sync — clicking
  ₹300/month CTA now pre-selects monthly in the modal

Also: qa-fix-plan-2026-04-01.md — full pre-launch QA report,
3 critical · 3 high · 4 medium · 2 low. Verdict: GO after this push.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
