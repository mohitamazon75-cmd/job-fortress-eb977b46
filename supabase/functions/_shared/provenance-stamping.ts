// Deno mirror of src/lib/provenance-stamping.ts. Keep in sync.
// Re-exported here so edge functions can import without reaching into src/.
// The TS source is identical — Deno understands plain TS just fine.

export * from "./provenance-stamping-impl.ts";
