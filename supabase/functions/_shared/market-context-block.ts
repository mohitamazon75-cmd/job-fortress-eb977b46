// Deno mirror of src/lib/market-context-block.ts. Kept as a re-export so the
// edge function and vitest both consume the identical pure helper.
export { buildMarketContextBlock } from "../../../src/lib/market-context-block.ts";
