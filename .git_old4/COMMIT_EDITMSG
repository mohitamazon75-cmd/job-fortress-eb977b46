feat: conversion funnel redesign — 3-card free arc + zero API cost for free users

Product decision: India market requires brutally cost-conscious free tier.

Free arc cut from 9 cards → 3 cards (all computed, zero extra API cost):
  1. Doom Clock (computed from scan data)
  2. Score Card (computed + viral share)
  3. Conversion Gate (new — blurred Pro preview, static, no API)

Removed from free tier (cost reasons):
  - best-fit-jobs: Perplexity/Tavily API call per user → Pro only
  - coach opt-in: triggers 3 LLM email nudges → Pro only
  - career-obituary prefetch: now gated behind isProUser → saves 1 LLM
    call per free user who never reaches that DEEP card

New components:
  - ConversionGateCard: emotional paywall with blurred Pro feature preview,
    score-based dynamic headline, social proof, dual CTA (year/scan)

Redesigned:
  - ProUpgradeModal: emotional copy ("You've seen the risk. Here's how to
    beat it."), social proof bar (avatars + star rating), felt-pain feature
    list, risk reversal ("7-day refund, no questions"), urgency copy
  - ProGateCard: blurred content preview instead of generic lock icon —
    shows realistic placeholder content per feature type (resume, skill,
    salary, interview, analysis)

Pro arc unchanged: 9 CORE + 10 DEEP cards, biggest_concern reordering
still applies, all LLM calls justified by ₹1,999/year revenue.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
