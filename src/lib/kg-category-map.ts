/**
 * Browser/Node mirror of supabase/functions/_shared/kg-category-map.ts.
 * Module is pure (no Deno imports), so we just re-export the implementation
 * verbatim. If you change one, change the other.
 */
export { getCategoryCandidates, fetchTaxonomyByCandidates } from "../../supabase/functions/_shared/kg-category-map";
